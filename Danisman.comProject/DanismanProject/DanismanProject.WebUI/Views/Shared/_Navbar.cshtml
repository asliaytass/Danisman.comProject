<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
    <div class="container-fluid mx-5 d-flex justify-content-between">
        <div>
            <a asp-area="" asp-controller="Home" asp-action="Index" class="navbar-brand fs-2" id="logo">
                <span id="logo-left">Danışman</span><span id="logo-right">.com</span>
            </a>
        </div>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav">
                @if (!User.Identity.IsAuthenticated)
                {
                    <li class="nav-item ">
                        <a asp-controller="Account" asp-action="Login" class="nav-link mx-2 " aria-current="page">
                            <i class="fa-solid fa-user"></i> Giriş
                            yap
                        </a>
                    </li>
                }


                else
                {
                    if (User.IsInRole("Admin"))
                    {

                        <li class="nav-item">
                            <a class="nav-link" asp-controller="MyAccount" asp-action="Index">@User.Identity.Name.ToUpper()</a>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="MyAccount" asp-action="Index">@User.Identity.Name.ToUpper()</a>
                        </li>

                    }


                    <li class="nav-item">
                        <a href="/account/logout" class="nav-link">Çıkış Yap</a>
                    </li>

                }


                <li class="nav-item">
                    <a class="nav-link" asp-controller="Home" asp-action="HelpContact">Yardım & İletişim</a>
                </li>


            </ul>


        </div>
    </div>
</nav>